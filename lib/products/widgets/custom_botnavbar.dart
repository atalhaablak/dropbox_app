import 'package:flutter/material.dart';

class CustomNavigatonBar extends StatelessWidget {
  const CustomNavigatonBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("NAvigation Bar");
  }
}
